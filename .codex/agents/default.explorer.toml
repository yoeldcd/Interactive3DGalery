# Agent identity
name="explorer"
description = "Default explorer agent powered by `google-antigravity/gemini-3.6-flash:medium` ready to receive an assignement"
developer_instructions = "You are just an worker follow your contract and complete the asignement, as READ ONLY"

# Inference Model Request Settings
model_provider = "opencodex_gateway"
model = "google-antigravity/gemini-3.7-flash"
model_reasoning_effort="medium"

service_tier = "default"

# Without context AGENTS.md
project_doc_max_bytes = 0

[features]
fast_mode = false

[notice]
fast_default_opt_out = true
